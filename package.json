{
  "name": "beta-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel sync wpstart",
    "wpstart": "wp-scripts start",
    "build": "npm-run-all --parallel wpbuild",
    "wpbuild": "wp-scripts build",
    "sync": "browser-sync start -p 'cosd-sandbox.local' --files '**/*.php' 'build/*.js' 'build/*.css'"
  },
  "keywords": [],
  "author": "Diego Corrales",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^28.0.0",
    "bootstrap": "^5.3.3",
    "browser-sync": "^3.0.2",
    "normalize.css": "^8.0.1",
    "npm-run-all": "^4.1.5"
  }
}
